══════════════════════════════════════════════════════════
STEP 4 — IMMEDIATE (before/during testing)
══════════════════════════════════════════════════════════

[ ] Add StandardResultsSetPagination (your code above)
    → Apply to JournalListCreateView.get()
    → Apply to AccountLedgerView.get()

[ ] Auto-create DocumentSequence on company creation
    → Add to generate_default_coa() in chartofaccounts/services.py
    → Add to generate_custom_coa() in chartofaccounts/services.py
    → Or create manually via Django Admin for existing companies

[ ] Update Company.has_financial_records() method
    → Change from "return False" to query LedgerEntry table

[ ] Register journals models in admin
    → journals/admin.py (ManualJournal, LedgerEntry)
    → companies/admin.py (TaxProfile, DocumentSequence, CurrencyExchangeRate)

[ ] Run full test guide (40+ Postman tests)

[ ] Fix any bugs found during testing


══════════════════════════════════════════════════════════
STEP 4 — DEFERRED (build later, documented in code)
══════════════════════════════════════════════════════════

[ ] contacts app — Contact model (customer, supplier, employee)
    → Then uncomment contact FK on ManualJournalLine
    → Then uncomment contact FK on LedgerEntry

[ ] CRUD views for TaxProfile (create, list, detail, update, deactivate)
    → Serializers already written, views not yet
    → Manage via Django Admin for now

[ ] CRUD views for CurrencyExchangeRate
    → Serializers already written, views not yet
    → Manage via Django Admin for now

[ ] CRUD views for DocumentSequence (update prefix/padding)
    → Serializers already written, views not yet
    → Manage via Django Admin for now

[ ] Lock date endpoint
    → PATCH /api/companies/<id>/settings/lock-date/
    → Serializer written, view not yet
    → Set via Django Admin for now

[ ] Account delete view — enforce "no journal entries" rule
    → chartofaccounts/views.py AccountDeleteView has a TODO
    → Add: has_entries = LedgerEntry.objects.filter(
          ledger_account=account).exists()

[ ] Pagination on older list views
    → companies/views.py CompanyListCreateView.get()
    → chartofaccounts/views.py AccountListCreateView.get()
    → chartofaccounts/views.py ClassificationListCreateView.get()
    → chartofaccounts/views.py ChartOfAccountsTreeView.get() (maybe not — tree is full)


══════════════════════════════════════════════════════════
FUTURE STEPS — NOTED DURING STEP 4 DESIGN
══════════════════════════════════════════════════════════

[ ] Step 5: Financial Reporting
    → Trial Balance
    → Balance Sheet
    → Income Statement (P&L)
    → General Ledger report
    → Period-end FX revaluation (unrealized gain/loss)

[ ] Step 6: Frontend (React)
    → Auth UI, Dashboard, CoA tree, Journal entry form
    → Settings pages for Tax, Currency, Sequences, Lock Date

[ ] Step 7+: Contacts, Sales, Purchase
    → Realized FX gain/loss (auto on invoice/bill payment settlement)
    → Invoice/Bill → LedgerEntry posting (same LedgerEntry table)

[ ] System account reassignment endpoint
    → PATCH /api/companies/<id>/system-accounts/<mapping_id>/
    → Deferred to Settings UI phase

[ ] Password reset flow (Step 1 deferred item)
    → Forgot password → email → reset link → new password

[ ] Reporting method (ACCRUAL/CASH/BOTH)
    → Field added to Company model
    → Actual filtering logic deferred to reporting step